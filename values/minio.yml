auth:
  rootUser: admin
  rootPassword: "password"
defaultBuckets: 'lagoon-files,restores' 
ingress:
  enabled: true
  hostname: minio-api.lagoon.jwrf.au
console:
  ingress:
    enabled: true
    hostname: minio.lagoon.jwrf.au
tls:
  enabled: true
  autoGenerated:
    enabled: true
    engine: helm
    certManager:
      existingIssuer: letsencrypt-prod
      existingIssuerKind: cluster-issuer
