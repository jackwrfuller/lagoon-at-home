auth:
  rootUser: admin
  rootPassword: "password"
defaultBuckets: 'lagoon-files,restores' 
ingress:
  enabled: true
  ingressClassName: nginx
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  tls: true
  hostname: minioapi.lagoon.jwrf.au
console:
  ingress:
    enabled: true
    ingressClassName: nginx
    tls: true
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    hostname: minio.lagoon.jwrf.au
